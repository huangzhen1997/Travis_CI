language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
notifications:
  email:
    recipients:
      - huangzhen9706@gmail.com
    on_success: always
    on_failure: always
script:
  - npm test
  - CI=false npm run build
after_success:
  - npm install -g firebase-tools
  - firebase deploy --token $FIREBASE_TOKEN --non-interactive
